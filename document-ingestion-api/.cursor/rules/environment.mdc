---
description: Environment and toolchain setup for running commands in this project
alwaysApply: true
---

# Environment & Toolchain

Java, Quarkus CLI, and Gradle are already on PATH — no manual sourcing of SDKman is needed.

Just run commands directly:

```bash
./gradlew <task>
```

- Java: 25 (Amazon Corretto)
- Quarkus CLI: 3.34.6
- Gradle: 9.3.1 via wrapper — always use `./gradlew`, never a system Gradle

## Database

The PostgreSQL database runs on the **host only** — it is NOT reachable from inside the dev container.

- Port: `5436`, user: `postgres`, password: `xxx`, database: `document-ingestion-db`
- Start it on the host with: `bash start-db.sh`
- Never attempt to start the DB or run Flyway migrations from inside the container
- If a DB connection error occurs, report it to the user and ask them to run it on the host

# File locks

If you see that there is a fileHashes.lock that is preventing you from you building, it probably
means that I have left the application running on host. You should ask me to stop it so that you can
proceed rather than deleting the lock.